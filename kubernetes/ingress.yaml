apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nick-portfolio-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: portfolio.nick.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: esp-service
            port:
              number: 80