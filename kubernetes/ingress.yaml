apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nick-portfolio-ingress
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nick-portfolio-service
            port:
              number: 80